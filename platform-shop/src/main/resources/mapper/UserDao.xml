<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="iris.platform.shop.dao.UserDao">

    <resultMap type="iris.platform.shop.entity.UserEntity" id="userMap">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="gender" column="gender"/>
        <result property="birthday" column="birthday"/>
        <result property="registerTime" column="register_time"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="lastLoginIp" column="last_login_ip"/>
        <result property="userLevelId" column="user_level_id"/>
        <result property="nickname" column="nickname"/>
        <result property="mobile" column="mobile"/>
        <result property="registerIp" column="register_ip"/>
        <result property="avatar" column="avatar"/>
        <result property="weixinOpenid" column="weixin_openid"/>
    </resultMap>

    <select id="queryObject" resultType="iris.platform.shop.entity.UserEntity">
		select
			id,
			username,
			password,
			gender,
			birthday,
			register_time,
			last_login_time,
			last_login_ip,
			user_level_id,
			nickname,
			mobile,
			register_ip,
			avatar,
			weixin_openid
		from tb_user
		where id = #{id}
	</select>

    <select id="queryList" resultType="iris.platform.shop.entity.UserEntity">
        select
        tb_user.id,
        tb_user.username,
        tb_user.password,
        tb_user.gender,
        tb_user.birthday,
        tb_user.register_time,
        tb_user.last_login_time,
        tb_user.last_login_ip,
        tb_user.user_level_id,
        tb_user.nickname,
        tb_user.mobile,
        tb_user.register_ip,
        tb_user.avatar,
        tb_user.weixin_openid,
        tb_user_level.name levelName
        from tb_user LEFT JOIN tb_user_level ON tb_user.user_level_id=tb_user_level.id
        WHERE 1=1
        <if test="username != null and username.trim() != ''">
            and username like concat('%',#{username},'%')
        </if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="queryTotal" resultType="int">
        select count(*) from tb_user
        WHERE 1=1
        <if test="username != null and username.trim() != ''">
            and username like concat('%',#{username},'%')
        </if>
    </select>

    <insert id="save" parameterType="iris.platform.shop.entity.UserEntity" useGeneratedKeys="true" keyProperty="id">
		insert into tb_user(
			`username`,
			`password`,
			`gender`,
			`birthday`,
			`register_time`,
			`last_login_time`,
			`last_login_ip`,
			`user_level_id`,
			`nickname`,
			`mobile`,
			`register_ip`,
			`avatar`,
			`weixin_openid`)
		values(
			#{username},
			#{password},
			#{gender},
			#{birthday},
			#{registerTime},
			#{lastLoginTime},
			#{lastLoginIp},
			#{userLevelId},
			#{nickname},
			#{mobile},
			#{registerIp},
			#{avatar},
			#{weixinOpenid})
	</insert>

    <update id="update" parameterType="iris.platform.shop.entity.UserEntity">
        update tb_user
        <set>
            <if test="username != null">`username` = #{username},</if>
            <if test="password != null">`password` = #{password},</if>
            <if test="gender != null">`gender` = #{gender},</if>
            <if test="birthday != null">`birthday` = #{birthday},</if>
            <if test="registerTime != null">`register_time` = #{registerTime},</if>
            <if test="lastLoginTime != null">`last_login_time` = #{lastLoginTime},</if>
            <if test="lastLoginIp != null">`last_login_ip` = #{lastLoginIp},</if>
            <if test="userLevelId != null">`user_level_id` = #{userLevelId},</if>
            <if test="nickname != null">`nickname` = #{nickname},</if>
            <if test="mobile != null">`mobile` = #{mobile},</if>
            <if test="registerIp != null">`register_ip` = #{registerIp},</if>
            <if test="avatar != null">`avatar` = #{avatar},</if>
            <if test="weixinOpenid != null">`weixin_openid` = #{weixinOpenid}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
		delete from tb_user where id = #{value}
	</delete>

    <delete id="deleteBatch">
        delete from tb_user where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>