<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="iris.platform.platformshop.mapper.EbStoreProductAttrValueMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="iris.platform.platformshop.entity.EbStoreProductAttrValue">
        <id column="suk" property="suk" />
        <result column="product_id" property="productId" />
        <result column="stock" property="stock" />
        <result column="sales" property="sales" />
        <result column="price" property="price" />
        <result column="image" property="image" />
        <result column="unique" property="unique" />
        <result column="cost" property="cost" />
        <result column="bar_code" property="barCode" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        product_id, suk, stock, sales, price, image, unique, cost, bar_code
    </sql>

</mapper>
