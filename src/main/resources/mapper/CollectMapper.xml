<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="iris.platform.shop.mapper.CollectMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="iris.platform.common.entity.Collect">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="value_id" property="valueId" />
        <result column="add_time" property="addTime" />
        <result column="is_attention" property="isAttention" />
        <result column="type_id" property="typeId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, value_id, add_time, is_attention, type_id
    </sql>

</mapper>
